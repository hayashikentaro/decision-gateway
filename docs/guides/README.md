# Implementation Guides

`docs/guides/` contains concise guidance for recurring implementation areas.

Current guides:

- `ui-style.md`: visual style guidance for Decision Gateway UI.
- `ui-components.md`: reusable control and component guidance for Decision Workspace surfaces.

Read these before changing shared UI patterns. Keep the guides lightweight and update them only when a rule is durable.

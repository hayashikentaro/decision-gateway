# Local Data

Decision Gateway stores development decision requests in `data/decision-requests.json`.

That file is intentionally ignored by Git because decision requests, materials, and decisions may contain sensitive context.
